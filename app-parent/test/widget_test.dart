import 'package:flutter_test/flutter_test.dart';

import 'package:monii_parent/main.dart';

void main() {
  test('app widget can be constructed', () {
    expect(const OpenParentalApp(), isA<OpenParentalApp>());
  });
}
